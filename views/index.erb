<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Task Time Calculator</title>
    <style>
      table {
        border-collapse: collapse;
        width: 100%;
        margin: 0 auto; /* Center the table horizontally */
      }

      th, td {
        border: 1px solid #ddd;
        padding: 8px;
        text-align: left;
      }

      th {
        background-color: #f2f2f2;
      }

      .title-container {
        display: flex;
        justify-content: space-between;
        align-items: center;
      }

      .title-table {
        width: 19%;
        margin-left: 40px; /* Adjust this value as needed */
      }

  </style>
</head>
<body>

  <div class="title-container">
    <h1>Task Time Calculator</h1>

    <table class="title-table">
      <thead>
        <tr>
          <th>Change Start</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <form id="centralStartTime" action="/change_central_start" method="post">
            <div style="display: flex; align-items: center;">
              <td><input type="text" name="central_start_hr" placeholder="Hour" required size="4">
              :
              <input type="text" name="central_start_min" placeholder="Min" required size="4">
              <input type="text" name="central_start_ampm" placeholder="AM/PM" required size="6">
              <button type="submit" class="save-button">Save</button></td>
            </div>
          </form>
        </tr>
      </tbody>
    </table>
  </div>

  <table>
    <thead>
      <tr>
        <th>Name</th>
        <th>Start</th>
        <th>End</th>
        <th>Duration (min)</th>
        <th>Notes</th>
        <th>Edit</th>
      </tr>
    </thead>
    <tbody>
      <% @tasks.each do |task| %>
        <tr>
          <td><%= task.name %></td>
          <td><%= format_start_time_helper(task) %></td>
          <td><%= format_end_time_helper(task) %></td>
          <td><%= task.duration %></td>
          <td><%= task.notes %></td>
          <td>
            <form action="/delete_task" method="post" style="display:inline;">
                <input type="hidden" name="task_id" value="<%= task.id %>">
                <button type="submit" class="delete-button">Delete</button>
            </form>
          </td>
        </tr>
      <% end %>
      <tr>
        <form id="newTaskForm" action="/save_task" method="post">
            <td><input type="text" name="new_task_name" placeholder="Task name" required ></td>
            <td>
              <% if prev_task %>
                <%= format_end_time_helper(prev_task) %>
              <% end %>
            </td> 
            <td></td>
            <td><input type="text" name="new_task_duration" placeholder="Duration (minutes)" required ></td>
            <td><input type="text" name="new_task_notes" placeholder="Notes"></td>
            <td><button type="submit" class="save-button">Save</button></td>
        </form>
      </tr>
    </tbody>
  </table>


</body>
</html>
